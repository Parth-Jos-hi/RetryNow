# Core data & ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
pyyaml>=6.0
tabulate>=0.9      # pandas DataFrame.to_markdown() (report rendering)

# Gradient boosting (optional — pipeline falls back to sklearn if missing)
xgboost>=1.7

# Web app (web/server.py is stdlib-only — nothing required here)

# LLM explainer (optional — pipeline runs in template mode without a key)
openai>=1.12
python-dotenv>=1.0

# Dev / tests
pytest>=7.4
ipykernel>=6.25
jupyter>=1.0